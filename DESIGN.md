# 信息花园 · 家庭信息聚合、检索与推送引擎

> 设计文档 v0.1 — 汇总目前为止的所有讨论结论，作为后续开发的底稿。

## 1. 背景与目标

公开搜索引擎（百度尤甚，谷歌稍好）和社交媒体上充斥着谣言、煽动性内容、偏见、冲突、花边新闻、诈骗，以及大量营销号和 SEO 垃圾。本项目要为家人建一个**真正的信息花园**：干净、可控、可信的信息获取环境。

结构上是"**多对一**"：抓取目标网站内容 → 用自己的**统一协议**归一化 → 整理后展示和推送。

## 2. 核心原则

1. **白名单优先。** 花园的干净来自"只种选过的植物"——精选信息源白名单，而不是试图给整个开放互联网做过滤。
2. **统一协议负责内容，原网页负责体验。** 结构化内容走协议；复杂交互/动画内容视为"应用而非文档"，用卡片 + webview 回退。
3. **推送克制。** 每日一份定量摘要日报 + 极少数即时提醒（天气、针对老人的诈骗预警）。一天推两三条以上，很快就会被家人静音。
4. **永不直接推原始链接。** 推自建的干净阅读页：大字体、无广告、家庭 token 免登录，页内保留"查看原文"入口。
5. **不做停留时长优化。** 排序透明（时间 + 手动源权重），每日有限内容池（约 30 条，读完即止），保留 10–20% 白名单内随机探索。

## 3. 总体架构

```
采集 → 清洗 → 存储（统一协议） → 过滤/评分 → 分发 → 消费端
```

| 层 | 方案 | 备选/备注 |
|---|---|---|
| 采集 | 白名单 RSS 轮询；RSSHub（万物皆可 RSS）；wewe-rss（公众号→RSS） | RSS-Bridge |
| 清洗 | 国内源：自建抓取（国内 IP + Readability）；海外源：Kagi Extract API | 见 §6 |
| 存储 | 统一内容协议入库（见 §4） | 快照可选 ArchiveBox |
| 检索 | Meilisearch 自建索引为主；Kagi Search API 兜底 | Typesense、SearXNG |
| 分发 | 规则过滤 + LLM 评分 → 按成员偏好表 + 每日配额 | 健康类内容仅允许白名单医疗源 |
| 推送 | Server酱 / WxPusher（微信，父母零门槛）；Bark（iOS）；ntfy | 企业微信群机器人 |
| 消费端 | 自建移动端优先阅读页 | 可选：规则化滑动信息流（见 §7） |

## 4. 统一内容协议（草案）

不发明新协议：基于 **JSON Feed / Atom + 扩展字段**。每条内容一个结构化对象：

```json
{
  "id": "sha256:…",
  "type": "article | image | video | audio | social | embed",
  "title": "…",
  "content_html": "…（清洗后的正文）",
  "summary": "…（LLM 摘要，推送卡片用）",
  "media": [
    { "type": "image | video | audio", "url": "…", "poster": "…", "duration": null }
  ],
  "source": { "name": "…", "feed": "…" },
  "original_url": "…",
  "published_at": "…",
  "fetched_at": "…",
  "tags": [],
  "score": null
}
```

- `type=embed`：无法降解为文档的交互内容，只存卡片元数据，消费端 webview 打开。
- 多媒体沿用成熟机制：RSS enclosure / Media RSS / JSON Feed attachments。

## 5. 文章推送流水线（MVP 主干）

大部分推送对象是**文章和网页**，不是视频。主干流程：

```
白名单 RSS 轮询
  → 清洗（正文提取）
  → 统一协议入库
  → 规则过滤 + LLM 评分
  → 按成员偏好分发（每日配额）
  → 微信卡片（标题 + 摘要）
  → 自建干净阅读页
```

## 6. Kagi 集成与搜索供应商策略

**定位：** Search API ↔ WebSearch，Extract API ↔ WebFetch。

**优点（已验证）：**
- 排序干净，付费即产品、无广告激励；
- **Search API 继承账号个性化设置**（域名拉黑/提权对 API 结果生效）——网页端调教好的干净排序，API 端原样享受。

**价格（2026）：** Search API $12/千次；Extract API $4/千页（URL → 干净 Markdown，单次 ≤10 URL）；预付 credits，可设 USD 消费上限，与搜索订阅分开计费。

**坏处与对策：**

| 坏处 | 对策 |
|---|---|
| 贵：AI 用法一个问题触发 5–10 次搜索，成本被放大 | 混合供应商：家人可见搜索走 Kagi（质量优先），Agent 机械性搜索走便宜供应商（Serper ~$1/千次、Brave ~$3/千次 + 每月 2000 次免费、Tavily $8/千次） |
| 慢：元搜索聚合多上游 + 仅海外节点 | 实测延迟；超时降级 |
| 中文盲区：搜索覆盖弱；Extract 从海外 IP 抓知乎/微博/公众号会被反爬拦截 | 国内源一律自建抓取（国内 IP + Readability），Extract 只管海外源 |
| 供应商风险：小公司、无 SLA、依赖 Google/Brave 等上游授权 | **SearchProvider 抽象层**：Kagi / Brave / 自建 SearXNG 均为可插拔实现 |
| 大陆可达性"未封锁不承诺" | 稳定出海通道 + 降级路径 |

预期校准：Kagi 给的是"垃圾**显著更少**、且可继续拉黑"，不是绝对无垃圾。

## 7. 消费端：不做抖音，留下手势

抖音四层拆解：短视频供给 / 零门槛滑动交互 / 无限流 / 停留时长优化算法。**保留前两层，丢弃后两层**——"一样的手势，不一样的目标函数"：

- 每日有限池（约 30 条），读完即止，无无限流；
- 手动源权重 + 喜欢/跳过做微调，无黑盒推荐；
- 视频侧（可选、后置）：Pinchflat / TubeArchivist 自动下载白名单频道 + Jellyfin；联邦式替代品 Loops 可关注。抖音站内内容基本不可提取，走"替代供给"策略。

## 8. 部署要点（国内）

- **国内服务器 + ICP 备案域名**：微信内打开快，且避免"非官方链接"提示——直接决定父母会不会点开第二次，备案要趁早办。
- 海外依赖（Kagi 等）可达性无承诺：预留稳定出海通道与无 Kagi 时的降级路径。

## 9. MVP 里程碑

- **M1** docker-compose 起 RSSHub + Miniflux/FreshRSS，白名单订阅跑通
- **M2** 统一协议入库 + 干净阅读页（移动端优先）
- **M3** Server酱日报推送（LLM 摘要 + 每日配额）
- **M4** Meilisearch 检索 + SearchProvider 抽象（接入 Kagi）
- 后续（可选）：规则化滑动信息流；视频侧替代供给

## 10. 开放问题

- [ ] 白名单初始源清单（按家庭成员兴趣分别列）
- [ ] LLM 摘要/评分用哪家模型，月成本预算
- [ ] 服务器与域名现状（是否已有、备案进度）
- [ ] Kagi 订阅方式（Starter / Family / 拼车）与 API credits 预算
